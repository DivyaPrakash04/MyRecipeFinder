# MCP Server Requirements - CORRECTED VERSIONS
# Enhanced MCP Server Dependencies (Free API Version)

# Core Flask dependencies
Flask==2.3.3
Flask-CORS==4.0.0
Flask-Limiter==3.5.0

# Environment and configuration
python-dotenv==1.0.0

# Database
SQLAlchemy==2.0.23

# API and HTTP requests
requests==2.31.0
aiohttp==3.9.1

# Note: The enhanced MCP server only needs the basic dependencies above
# The langchain dependencies are optional and may cause version conflicts